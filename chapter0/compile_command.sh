gcc basic_program.c -o output

./output

# it contains two lines the first one compiles the program and the second one executes the binary file that is generated
#you can run these commands on the terminal to generate the same output
